name: modawanat_app
description: نظام مدونة الحسابات
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0

  # أدوات التطبيق
  url_launcher: ^6.2.5
  pdf: ^3.10.8
  printing: ^5.12.0
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true
